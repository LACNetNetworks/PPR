# Inherit the bare minimum; change to "keycloak" if you prefer their base layout assets.
parent=base
# Load our CSS (multiple comma-separated paths allowed)
styles=css/custom.css

# Dev convenience (turn off in prod or omit)
cacheThemes=false
cacheTemplates=false
